// // Live update of video feed
// const videoFeed = document.getElementById('videoFeed');

// setInterval(() => {
//     const timestamp = new Date().getTime(); // cache buster
//     videoFeed.src = `/video_feed?${timestamp}`;
// }, 200);